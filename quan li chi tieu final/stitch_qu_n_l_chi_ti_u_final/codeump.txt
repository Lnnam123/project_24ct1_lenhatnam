@startuml
!theme plain
skinparam ActivityBackgroundColor #E3F2FD
skinparam ActivityBorderColor #1E88E5

start
:Mở ứng dụng (App Khách hàng);

if (Đã đăng nhập?) then (Chưa)
  :Nhập thông tin xác thực;
  :Kiểm tra tài khoản trong Database;
else (Rồi)
endif

:Hiển thị Màn hình chính (Dashboard);
note right: Hiển thị Tổng số dư và\nNgân sách hiện tại

fork
  :Chọn "Thêm giao dịch thủ công";
  :Nhập Số tiền, Danh mục, Ghi chú;
fork again
  :Chọn "Thêm bằng AI/Camera";
  :Cung cấp Hình ảnh hoặc Giọng nói;
  :Gọi API xử lý AI (OCR/NLP);
  :Trả về dữ liệu đã bóc tách;
  :Người dùng kiểm tra và chỉnh sửa;
fork again
  :Chọn "Xem báo cáo";
  :Truy xuất dữ liệu giao dịch;
  :Hiển thị Biểu đồ (fl_chart);
  stop
fork again
  :Chọn "Quản lý ngân sách";
  :Thiết lập hạn mức chi tiêu mới;
end fork

if (Xác nhận lưu?) then (Có)
  :Gửi yêu cầu lên máy chủ;
  :Cập nhật các bảng SQL (Giao dịch, Ngân sách);
  :Tính toán lại tổng số dư;
  :Hiển thị thông báo thành công;
else (Hủy bỏ)
endif

:Quay lại Màn hình chính;
stop
@enduml